import React from 'react'
import ReactDOM from 'react-dom/client'
import UnifiedAppointments from './UnifiedAppointments.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <UnifiedAppointments />
  </React.StrictMode>
)
